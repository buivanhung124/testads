/// `GSXCore` đi kèm luôn, để app chỉ phải `import GSXAds`.
///
/// `EntitlementCenter` là chỗ hai module gặp nhau — tầng quảng cáo đọc, tầng
/// thanh toán ghi — nên bắt người dùng nhớ thêm một dòng `import GSXCore` chỉ
/// để chạm vào nó là một sự phiền không có lý do.
@_exported import GSXCore
